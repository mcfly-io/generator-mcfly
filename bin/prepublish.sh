#!/bin/bash
#which bower || npm install -g bower || sudo npm install -g bower
#which istanbul || npm install -g istanbul || sudo npm install -g istanbul
#which mocha || npm install -g mocha || sudo npm install -g mocha
#which gulp || npm install -g gulp || sudo npm install -g gulp
#which codeclimate || npm install -g codeclimate-test-reporter || sudo npm install -g codeclimate-test-reporter
echo "prepublish executed sucessfully"
